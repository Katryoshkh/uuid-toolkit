# uuid-toolkit
uuid-toolkit is a command-line utility for generating and validating UUIDs (v1, v3, v4, v5). uuid-toolkit provides deterministic and non-deterministic generation, namespace support, input validation, and cross-platform execution with a clean and predictable interface.
